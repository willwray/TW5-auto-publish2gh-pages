language: node_js
node_js:
  - "node"
before_script:
  - tiddlywiki --version
  - pwd
  - ls
script: tiddlywiki ./*wiki --version --verbose --build

after_success:
  - bash ./publish.sh

branches:
  only:
    - master
